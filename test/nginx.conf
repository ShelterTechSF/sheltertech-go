events {}

http {
    server {
        listen 80;
        server_name jwks-server;

        root /usr/share/nginx/html;

        location /jwks.json {
            default_type application/json;
        }

        location /.well-known/openid-configuration {
            default_type application/json;
        }
    }
}
